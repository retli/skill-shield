# 代码审计报告模板

## {项目名称} — 安全审计报告

### 项目概览

| 属性 | 值 |
|------|-----|
| 项目名称 | |
| 审计模式 | standard / deep |
| 审计范围 | |
| 技术栈 | |
| 文件总数 | |
| EALOC | Tier1: {N} files / Tier2: {N} files / Tier3: {N} files |
| 审计日期 | |

---

### 漏洞汇总

| 严重程度 | 数量 | 说明 |
|----------|------|------|
| 🔴 Critical (DKTSS 9-10) | | 必须立项修复。外网无签权/可脱库/RCE |
| 🟡 High (DKTSS 7-8) | | 下个发版周期解决。需特定条件或弱交互 |
| 🔵 Medium (DKTSS 4-6) | | 有限影响或利用阻力极大 (如内网+强鉴权) |
| ⚪ Low (DKTSS 1-3) | | 防御纵深缺乏或纯理论边界问题 |

### 漏洞列表

| # | 验证状态 | 严重程度 | 漏洞类型 | 位置 |
|---|--------|---------|---------|------|
| 1 | CONFIRMED / HYPOTHESIS | | | |

---

### 漏洞详情

> **报告规范强红线**：
> 下列字段缺一不可。缺少任意核心要素，本漏洞块作废，打回返工。

#### [严重程度] 漏洞类型 — 文件名:行号

**1. 基本信息**

| 属性 | 值 |
|------|-----|
| 验证状态 | CONFIRMED (实锤) / HYPOTHESIS (疑似或有条件假说) |
| 漏洞类型 | SQL注入 / XSS / RCE / SSRF / ... |
| CWE | CWE-XXX |
| DKTSS 评分 | 必须列出算式: `Base(X) - Friction(Y) + Weapon(Z) + Ver(W) = 分数` |
| 受影响组件 | 类名/模块名 |
| 文件位置 | path/to/file:行号 |

**2. 触发前置条件**

> 引用 `security/` 目录下的细则或阐述实际情境 (如需特定依赖库、框架需开启 Debug、需什么版本的 JDK)
- 条件1: ...
- 条件2: ...

**3. 所需权限/阻力 (Friction 映射)**
> 无需认证 / 低权限用户角色 / 需要进入内网 / 需要管理员身份...

**4. 漏洞原理分析过程**
> (步骤 >= 2)
1. 
2. 

**5. 核心代码证据**



必须从实际的 `Read` 输出中提取原样代码，严禁编造：
```java
// 代码片段 (必须附带源文件和行号)
```

**6. 完整调用链 (Call Chain)**
> Source -> Sink 完整链路，每跳必带 文件:行号
```text
[Source] file:行号
  ↓
[Trace] file:行号
  ↓
[Sink] file:行号
```

**7. PoC (概念验证)**

> **注意**: 必须遵循 `core/poc_generation.md` 规范，使用完整的 HTTP 请求数据包格式。

```http
POST /api/vuln HTTP/1.1
Host: <target-host>
Content-Type: application/x-www-form-urlencoded

{完整的 HTTP 攻击 payload}
```

**8. 业务实质影响**
> 脱库风险/接管服务器风险/导致内网其他终端受影响/造成拒绝服务等，贴合具体业务。

**9. 修复建议**
> 至少1条具体的修复意见（推荐代码/配置项）
```
安全代码或缓解配置示例
```

---

### 覆盖矩阵

| # | 维度 | 已覆盖 | 发现数 |
|---|------|--------|--------|
| D1 | 注入 | ✅/⚠️/❌ | |
| D2 | 认证 | | |
| D3 | 授权 | | |
| D4 | 反序列化 | | |
| D5 | 文件操作 | | |
| D6 | SSRF | | |
| D7 | 加密 | | |
| D8 | 配置 | | |
| D9 | 业务逻辑 | | |
| D10 | 供应链 | | |

覆盖率: {N}/10

---

### 审计方法

- **审计框架**: Code Auditor（EALOC 权重 + 双轨并行）
- **轨道 A（控制建模法）**: 枚举敏感操作，验证安全控制完整性
- **轨道 B（数据流分析）**: 追踪 Source → Sink 路径，验证净化有效性
- **工具链**: scan_sinks.py 粗筛 → LSP/Grep 深度追踪 → 手动验证
